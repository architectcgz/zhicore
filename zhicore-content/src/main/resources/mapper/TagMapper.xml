<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhicore.content.infrastructure.repository.mapper.TagMapper">

    <!-- 获取热门标签（按文章数量排序） -->
    <select id="findHotTags" resultType="java.util.HashMap">
        SELECT 
            ts.tag_id,
            ts.post_count
        FROM tag_stats ts
        WHERE ts.post_count > 0
        ORDER BY ts.post_count DESC
        LIMIT #{limit}
    </select>

    <!-- 批量查询文章的标签（避免 N+1 查询） -->
    <select id="selectTagsByPostIds" resultType="java.util.HashMap">
        SELECT 
            pt.post_id,
            t.id,
            t.name,
            t.slug,
            t.description,
            t.created_at,
            t.updated_at
        FROM post_tags pt
        INNER JOIN tags t ON pt.tag_id = t.id
        WHERE pt.post_id IN
        <foreach collection="postIds" item="postId" open="(" separator="," close=")">
            #{postId}
        </foreach>
        ORDER BY pt.post_id, pt.created_at DESC
    </select>

</mapper>
