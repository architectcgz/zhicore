<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhicore.content.infrastructure.repository.mapper.PostMapper">

    <!-- 根据条件查询文章列表 -->
    <select id="selectByConditions" resultType="com.zhicore.content.infrastructure.repository.po.PostPO">
        SELECT * FROM posts
        <where>
            <if test="keyword != null and keyword != ''">
                AND title LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = CAST(#{status} AS SMALLINT)
            </if>
            <if test="authorId != null and authorId != ''">
                AND owner_id = #{authorId}
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 根据条件统计文章数量 -->
    <select id="countByConditions" resultType="long">
        SELECT COUNT(*) FROM posts
        <where>
            <if test="keyword != null and keyword != ''">
                AND title LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = CAST(#{status} AS SMALLINT)
            </if>
            <if test="authorId != null and authorId != ''">
                AND owner_id = #{authorId}
            </if>
        </where>
    </select>

</mapper>
