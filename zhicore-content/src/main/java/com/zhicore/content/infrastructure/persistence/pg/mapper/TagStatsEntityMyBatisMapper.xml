<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhicore.content.infrastructure.persistence.pg.mapper.TagStatsEntityMyBatisMapper">

    <!-- 更新标签统计（upsert 操作） -->
    <insert id="upsertTagStats">
        INSERT INTO tag_stats (tag_id, post_count, updated_at)
        SELECT 
            #{tagId},
            COUNT(*),
            CURRENT_TIMESTAMP
        FROM post_tags
        WHERE tag_id = #{tagId}
        ON CONFLICT (tag_id) DO UPDATE SET
            post_count = EXCLUDED.post_count,
            updated_at = EXCLUDED.updated_at
    </insert>

    <!-- 批量更新标签统计 -->
    <insert id="batchUpsertTagStats">
        INSERT INTO tag_stats (tag_id, post_count, updated_at)
        SELECT 
            pt.tag_id,
            COUNT(*),
            CURRENT_TIMESTAMP
        FROM post_tags pt
        WHERE pt.tag_id IN
        <foreach collection="tagIds" item="tagId" open="(" separator="," close=")">
            #{tagId}
        </foreach>
        GROUP BY pt.tag_id
        ON CONFLICT (tag_id) DO UPDATE SET
            post_count = EXCLUDED.post_count,
            updated_at = EXCLUDED.updated_at
    </insert>

    <!-- 删除标签统计 -->
    <delete id="deleteTagStats">
        DELETE FROM tag_stats WHERE tag_id = #{tagId}
    </delete>

</mapper>
