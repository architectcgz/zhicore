# 外部服务熔断与降级

该目录用于存放 `zhicore-content` 调用外部服务时的熔断、限流、降级相关实现。

建议按职责拆分子包：

- `guard`：外部调用保护层（对外部服务调用进行 Sentinel 资源保护与统一入口封装）。
- `fallback`：降级处理与 `blockHandler` 实现。

典型接入目标：

- ID 生成服务调用
- 用户资料服务调用
- 上传/文件服务调用

职责边界建议：

- 业务编排逻辑放在 `application.service`
- 熔断、降级、限流等技术实现放在本目录
