<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhicore.content.infrastructure.persistence.pg.mapper.PostTagEntityMyBatisMapper">

    <!-- 批量插入 Post-Tag 关联 -->
    <!-- 使用 MERGE 语句兼容 H2 和 PostgreSQL -->
    <insert id="insertBatch" parameterType="java.util.List">
        <foreach collection="list" item="item" separator=";">
            MERGE INTO post_tags (post_id, tag_id, created_at)
            KEY (post_id, tag_id)
            VALUES (#{item.postId}, #{item.tagId}, #{item.createdAt})
        </foreach>
    </insert>

    <!-- 批量查询文章的标签ID（避免 N+1 查询） -->
    <select id="selectByPostIds" resultType="com.zhicore.content.infrastructure.persistence.pg.entity.PostTagEntity">
        SELECT post_id, tag_id, created_at
        FROM post_tags
        WHERE post_id IN
        <foreach collection="postIds" item="postId" open="(" separator="," close=")">
            #{postId}
        </foreach>
        ORDER BY post_id, created_at DESC
    </select>

</mapper>
