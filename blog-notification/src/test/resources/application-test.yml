spring:
  application:
    name: blog-notification-test
  
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false

# 禁用 Knife4j 在测试环境
knife4j:
  enable: false

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
